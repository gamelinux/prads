#### PRADS INSTALL

# TODO : apt-get install prads

# On a Ubuntu system:
$ aptitude install libnet-pcap-perl libgetopt-long-descriptive-perl git libdbd-sqlite3-perl
$ git clone git://github.com/gamelinux/prads.git
$ sudo mkdir -p /etc/prads
$ sudo ln -s $PWD/prads/etc/* /etc/prads/
$ sudo perl prads/sbin/prads.pl --help

